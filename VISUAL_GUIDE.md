# Visual Guide: Fix and Ignore Buttons in Action

## 🖼️ User Interface Flow

### Scanner Page Layout

```
┌─────────────────────────────────────────────────────────────┐
│ WP Harden > Security Scanner                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  [🔍 Start Full Scan]  [⚡ Quick Scan]                      │
│                                                              │
│  Automatic Scans: [Daily ▼]                                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Latest Scan Results                                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Scan Date: 2024-01-15 10:30:45                            │
│  Total Issues: 5                                            │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Core Integrity                                             │
│  Status: Failed                                             │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ [✓] │ Issue          │ Severity │ Details   │ Action │  │
│  ├─────┼────────────────┼──────────┼───────────┼────────┤  │
│  │ [ ] │ wp-config.php  │ High     │ Perms:    │ [Fix]  │  │
│  │     │ Insecure perms │          │ 0644      │[Ignore]│  │
│  │     │                │          │ Need:0600 │        │  │
│  ├─────┼────────────────┼──────────┼───────────┼────────┤  │
│  │ [ ] │ Debug Mode     │ Medium   │ WP_DEBUG  │ [Fix]  │  │
│  │     │ Enabled        │          │ is true   │[Ignore]│  │
│  └─────┴────────────────┴──────────┴───────────┴────────┘  │
│                                                              │
│  [🔧 Fix Selected]  [👁️ Ignore Selected]                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🎬 Action Sequence: Fix Button Click

### Step 1: Initial State
```
User sees the issue row:
┌────────────────────────────────────────────────────┐
│ [ ] │ wp-config.php     │ High  │ Perms: 0644 │    │
│     │ Insecure perms    │       │ Need: 0600  │    │
│     │                   │       │             │    │
│     └─────────────────────────────────────────┘    │
│     [Fix] [Ignore]                                 │
└────────────────────────────────────────────────────┘
```

### Step 2: User Clicks "Fix"
```
Confirmation dialog appears:
┌─────────────────────────────────────┐
│ Confirm                             │
├─────────────────────────────────────┤
│ Attempt to automatically fix this   │
│ issue?                              │
│                                     │
│         [Cancel]  [OK]              │
└─────────────────────────────────────┘
```

### Step 3: Processing
```
Button state changes:
┌────────────────────────────────────────────────────┐
│ [ ] │ wp-config.php     │ High  │ Perms: 0644 │    │
│     │ Insecure perms    │       │ Need: 0600  │    │
│     │                   │       │             │    │
│     └─────────────────────────────────────────┘    │
│     [Fixing...] [Ignore]                           │
│      (disabled)  (disabled)                        │
└────────────────────────────────────────────────────┘

AJAX Request:
POST /wp-admin/admin-ajax.php
{
  action: 'wph_fix_issue',
  nonce: 'abc123...',
  issue_type: 'core_integrity',
  issue_data: '{"file":"wp-config.php", ...}'
}
```

### Step 4: Success Response
```
Success notice appears:
┌─────────────────────────────────────────────────────┐
│ ✅ wp-config.php permissions updated to 0600        │
└─────────────────────────────────────────────────────┘

Row fades out:
┌────────────────────────────────────────────────────┐
│ [ ] │ wp-config.php     │ High  │ Perms: 0644 │    │
│     │ Insecure perms    │       │ Need: 0600  │    │
│     │ (fading...)       │       │ (opacity)   │    │
└────────────────────────────────────────────────────┘
```

### Step 5: Complete
```
Row removed, issue count updates:
┌─────────────────────────────────────────────────────┐
│ Total Issues: 4  (was 5)                           │
└─────────────────────────────────────────────────────┘

(Row is gone from the table)
```

## 🎬 Action Sequence: Ignore Button Click

### Step 1: User Clicks "Ignore"
```
Prompt appears:
┌─────────────────────────────────────┐
│ Why are you ignoring this issue?    │
│ (optional)                          │
│                                     │
│ [False positive - testing server__] │
│                                     │
│         [Cancel]  [OK]              │
└─────────────────────────────────────┘
```

### Step 2: Processing
```
Button state changes:
┌────────────────────────────────────────────────────┐
│ [Fix] [Ignoring...]                                │
│ (disabled) (disabled)                              │
└────────────────────────────────────────────────────┘

AJAX Request:
POST /wp-admin/admin-ajax.php
{
  action: 'wph_ignore_issue',
  nonce: 'abc123...',
  issue_type: 'core_integrity',
  issue_data: '{"file":"wp-config.php", ...}',
  reason: 'False positive - testing server'
}
```

### Step 3: Database Storage
```
Record inserted:
INSERT INTO wp_wph_ignored_issues
  (issue_type, issue_key, issue_data, ignored_by, 
   ignored_at, reason)
VALUES
  ('core_integrity', 
   'a1b2c3d4...', 
   '{"file":"wp-config.php",...}',
   1,
   '2024-01-15 10:35:22',
   'False positive - testing server')
```

### Step 4: Success
```
Success notice + row removal:
┌─────────────────────────────────────────────────────┐
│ ✅ Issue ignored successfully.                      │
└─────────────────────────────────────────────────────┘

Row fades out and is removed from table.
```

## 🎬 Bulk Action Sequence

### Step 1: User Selects Multiple Issues
```
┌──────────────────────────────────────────────────────┐
│ [✓] │ All                                            │
├─────┼────────────────────────────────────────────────┤
│ [✓] │ wp-config.php perms │ High   │ [Fix][Ignore] │
├─────┼─────────────────────────────────────────────────┤
│ [✓] │ Debug mode enabled  │ Medium │ [Fix][Ignore] │
├─────┼─────────────────────────────────────────────────┤
│ [ ] │ Default DB prefix   │ Low    │ [Fix][Ignore] │
└──────────────────────────────────────────────────────┘

[🔧 Fix Selected]  [👁️ Ignore Selected]
```

### Step 2: User Clicks "Fix Selected"
```
Confirmation:
┌─────────────────────────────────────┐
│ Attempt to fix 2 selected issues?  │
│                                     │
│         [Cancel]  [OK]              │
└─────────────────────────────────────┘
```

### Step 3: Bulk Processing
```
Button changes:
[Fixing...] [👁️ Ignore Selected]
(disabled)    (disabled)

AJAX Request:
POST /wp-admin/admin-ajax.php
{
  action: 'wph_bulk_fix',
  nonce: 'abc123...',
  issues: [
    {
      type: 'core_integrity',
      data: {file: 'wp-config.php', ...}
    },
    {
      type: 'core_integrity',
      data: {setting: 'WP_DEBUG', ...}
    }
  ]
}
```

### Step 4: Batch Results
```
Success notice:
┌─────────────────────────────────────────────────────┐
│ ✅ Fixed 1 of 2 issues.                             │
└─────────────────────────────────────────────────────┘

Details in console:
Console:
  ✓ wp-config.php: Success
  ✗ WP_DEBUG: Manual edit required

Both rows fade out and are removed.
```

## 📊 Data Flow Diagram

```
┌──────────┐
│  User    │
│  Action  │
└────┬─────┘
     │
     ▼
┌────────────────┐
│  JavaScript    │  1. Capture data from attributes
│  Event Handler │  2. Show confirmation/prompt
└────┬───────────┘  3. Disable buttons
     │              4. Make AJAX call
     │
     ▼
┌────────────────┐
│  WordPress     │  1. Verify nonce
│  AJAX Handler  │  2. Check capability
│  (PHP)         │  3. Sanitize input
└────┬───────────┘  4. Call scanner method
     │
     ▼
┌────────────────┐
│  Scanner       │  Fix:    Attempt file changes
│  Method        │  Ignore: Insert to database
│  (PHP)         │  Both:   Log to activity log
└────┬───────────┘
     │
     ▼
┌────────────────┐
│  Response      │  {success: true/false,
│  JSON          │   message: "...",
└────┬───────────┘   data: {...}}
     │
     ▼
┌────────────────┐
│  JavaScript    │  1. Show notice
│  Success       │  2. Remove row with fadeout
│  Handler       │  3. Update issue count
└────────────────┘  4. Re-enable buttons
```

## 🗄️ Database State

### Before Ignore Action
```sql
SELECT * FROM wp_wph_ignored_issues;
Empty set (0 rows)
```

### After Ignore Action
```sql
SELECT * FROM wp_wph_ignored_issues;

+----+-------------+-----------+----------------+------------+
| id | issue_type  | issue_key | ignored_by     | ignored_at |
+----+-------------+-----------+----------------+------------+
| 1  | core_...    | a1b2c3... | 1              | 2024-01... |
+----+-------------+-----------+----------------+------------+
| reason: "False positive - testing server"                  |
+----+-------------+-----------+----------------+------------+
```

## 🎨 Visual States

### Button States

**Normal State**
```
[Fix] [Ignore]
```

**Hover State**
```
[Fix] [Ignore]
 ^^^ (slightly darker)
```

**Processing State**
```
[Fixing...] [Ignore]
 (disabled)  (disabled)
```

**After Success**
```
(Row fades out and disappears)
```

## 🔔 Notice Types

### Success Notice
```
┌────────────────────────────────────────────┐
│ ✅ wp-config.php permissions updated to   │
│    0600                                    │
│                                      [×]   │
└────────────────────────────────────────────┘
(Green background, auto-dismiss after 5s)
```

### Error Notice
```
┌────────────────────────────────────────────┐
│ ❌ Failed to update permissions. Please   │
│    do this via FTP/SSH: chmod 600         │
│                                      [×]   │
└────────────────────────────────────────────┘
(Red background, auto-dismiss after 5s)
```

## 📱 Responsive Behavior

### Desktop View
```
Issue | Severity | Details      | [Fix] [Ignore]
```

### Mobile View (Stacked)
```
Issue: wp-config.php
Severity: High
Details: Permissions 0644
Actions: [Fix] [Ignore]
```

## 🎯 User Journey

```
User visits Scanner page
        ↓
Runs security scan
        ↓
Reviews issues found
        ↓
    /           \
   /             \
Fix It        Ignore It
  ↓               ↓
Click Fix      Click Ignore
  ↓               ↓
Confirm         Enter reason
  ↓               ↓
Processing      Processing
  ↓               ↓
 Success         Success
  ↓               ↓
Row removed    Row removed
  ↓               ↓
Issue count updates
        ↓
Scan results cleaner
```

## 🔍 Technical Details Visible to User

### Before Fix
```
Current: 0644
Expected: 0600
```

### During Fix
```
[Fixing...]
(button disabled)
```

### After Fix
```
✅ Permissions updated to 0600
(row disappears)
```

## 💡 User Education

The interface teaches users:
1. **What's wrong**: Clear issue descriptions
2. **How serious**: Severity badges (Critical, High, Medium, Low)
3. **What to do**: Action buttons with clear labels
4. **What happened**: Success/error messages with details
5. **Progress**: Visual feedback during processing

---

## 🎬 End Result

Users can now effectively manage their security issues with:
- ✅ Clear visual feedback
- ✅ Smooth interactions
- ✅ No page reloads
- ✅ Comprehensive error handling
- ✅ Professional WordPress-style UI

**The scanner is now a fully functional security management tool!**
